<?php

use Krystal\Form\Element;
defined('KRYSTAL') or die;

?>

<div class="btn-toolbar">
	<div class="btn-group">
	  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
		<i class="glyphicon glyphicon-list"></i> <?php $this->show('Menu'); ?> <span class="caret"></span>
	  </button>
	  <ul class="dropdown-menu">
         <li><a href="<?php echo $this->url('Faq:Admin:Faq@addAction'); ?>"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Add new FAQ'); ?></a></li>
		 <li><a href="<?php echo $this->url('Faq:Admin:Category@addAction'); ?>"><i class="glyphicon glyphicon-folder-open"></i> <?php $this->show('Add a category'); ?></a></li>
		 <li class="divider"></li>
		 <li class="dropdown-header"><?php $this->show('Actions'); ?></li>

		 <?php if (!empty($faqs)): ?>
		 
		 <li><a href="#" data-button="save-changes" data-url="<?php echo $this->url('Faq:Admin:Faq@tweakAction'); ?>"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
		 <li><a href="#" data-button="remove-selected" data-url="<?php echo $this->url('Faq:Admin:Faq@deleteAction', null); ?>"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
		 <?php else: ?>
		 
		 <li class="disabled"><a><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
		 <li class="disabled"><a><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
		 
		 <?php endif; ?>
	  </ul>
	</div>
</div>


<form>
    <div class="row">
        <div class="col-lg-8">
            <?php $this->loadPartial('items-per-page', array('caption' => 'FAQ')); ?>
            
            <table class="table table-hover table-bordered table-striped table-condensed">
                <thead>
                    <tr>
                        <th class="text-center">
                            <?php if (!empty($faqs)): ?>
                            <input type="checkbox" />
                            <?php endif; ?>
                        </th>
                        <th class="text-center"><?php $this->show('#'); ?></th>
                        <th class="text-center"><?php $this->show('Question'); ?></th>
                        <th class="text-center"><?php $this->show('Published'); ?></th>
                        <th class="text-center"><?php $this->show('Order'); ?></th>
                        <th class="text-center"><?php $this->show('Actions'); ?></th>
                    </tr>
                </thead>
                
                <?php if (!empty($faqs)): ?>
                <tbody>
                    <?php foreach ($faqs as $faq): ?>
                    <tr>
                        <td class="text-center">
                            <input type="checkbox" name="toDelete[<?php echo $faq->getId(); ?>]" />
                        </td>
                        <td class="text-center text-muted"><?php echo $faq->getId(); ?></td>
                        <td><?php echo $faq->getQuestion(); ?></td>
                        <td class="text-center">
                            <?php echo Element::checkbox(sprintf('published[%s]', $faq->getId()), $faq->getPublished()); ?>
                        </td>
                        <td class="text-center col-md-1">
                            <?php echo Element::number(sprintf('order[%s]', $faq->getId()), $faq->getOrder(), array('class' => 'form-control', 'min' => '0')); ?>
                        </td>
                        <td class="text-center">
                            <a data-toggle="tooltip" data-placement="left" data-original-title="<?php $this->show('Edit this pair'); ?>" data-button="edit" href="<?php echo $this->url('Faq:Admin:Faq@editAction', $faq->getId()); ?>"><i class="glyphicon glyphicon-pencil"></i></a>
                            <a data-toggle="tooltip" data-placement="left" data-original-title="<?php $this->show('Remove this pair'); ?>" data-button="delete" data-url="<?php echo $this->url('Faq:Admin:Faq@deleteAction', $faq->getId()); ?>" href="#" ><i class="glyphicon glyphicon-remove"></i></a>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
                <?php endif; ?>
            </table>

            <?php if (empty($faqs)): ?>
            <div class="center-block">
                <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No faqs'); ?></h2>
            </div>
            <?php endif; ?>

            <?php $this->loadPartial('paginator'); ?>
        </div>

        <div class="col-lg-4">
            <?php if (!empty($categories)): ?>
            <h3><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Categories'); ?> <small>(<?php echo count($categories); ?>)</small></h3>

            <table class="table table-hover table-bordered table-striped table-condensed">
                <thead>
                    <tr>
                        <th class="text-center"><?php $this->show('#'); ?></th>
                        <th class="text-center"><?php $this->show('Name'); ?></th>
                        <th class="text-center"><?php $this->show('Order'); ?></th>
                        <th class="text-center"><?php $this->show('Actions'); ?></th>
                    </tr>
                </thead>

                <tbody>
                    <?php foreach ($categories as $category): ?>
                    <tr class="<?php echo $category->getId() == $categoryId ? 'info' : null; ?>">
                        <td class="text-center text-muted"><?php echo $category->getId(); ?></td>
                        <td>
                            <a href="<?php echo $this->mapUrl('Faq:Admin:Faq@categoryAction', array($category->getId()), 0); ?>"><?php echo $category->getName(); ?></a>
                        </td>
                        <td class="text-center"><?php echo $category->getOrder(); ?></td>
                        <td class="text-center">
                            <a data-toggle="tooltip" data-placement="left" data-original-title="<?php $this->show('Edit this pair'); ?>" data-button="edit" href="<?php echo $this->url('Faq:Admin:Category@editAction', $category->getId()); ?>"><i class="glyphicon glyphicon-pencil"></i></a>
                            <a data-toggle="tooltip" data-placement="left" data-original-title="<?php $this->show('Remove this pair'); ?>" data-button="delete" data-url="<?php echo $this->url('Faq:Admin:Category@deleteAction', $category->getId()); ?>" href="#" ><i class="glyphicon glyphicon-remove"></i></a>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
            <?php endif; ?>
        </div>
    </div>
</form>
